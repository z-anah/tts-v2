Here is a **clean final prompt** generated from your QCM answers.

---

## **Final Prompt (Ready to use for your Vue project)**

Build a feature that generates **Arabic-learning QCM exercises based only on images**, following this specification:

1. The component must display **images only**, no Arabic text by default.
2. The teacher’s question should be provided as **audio/voice playback**.
3. The user answers by **choosing from multiple images**, arranged clearly in a grid-like layout.
4. Vocabulary data will be provided manually as **name + image URL**.
5. The system does **not** track long-term progress.
6. Questions must appear in a **random order**, and answer choices should also be shuffled.
7. The layout should look like a **brick/grid style**.

The goal is to recreate the private Inlingua style learning flow:
the teacher says a word (audio), the learner identifies it by selecting the correct image.

---